algorithm: grpo
aggregation: balanced
verl_loss_mode: vanilla
verl_loss_agg_mode: token-mean
use_kl_loss: true
clip_ratio_low: 0.2
clip_ratio_high: 0.2
balanced:
  subset_reduction: token_mean
  subset_weighting: sequence_count
  zero_advantage: ignore
